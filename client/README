Dependencies
============

- nodenv
- Ruby and bundler (rvm or rbenv would be a good idea)

Getting started
---------------

```
bundle install
npm install
bower install

# Build
gulp

# Serve locally
gulp serve
``` 
